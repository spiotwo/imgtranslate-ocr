{
  "name": "image-translator-ocr",
  "version": "2.3.0",
  "private": true,
  "description": "Firefox extension. Build script bundles chrome-lens-ocr into lens-bundle.js.",
  "scripts": {
    "build": "esbuild entry.mjs --bundle --format=iife --outfile=lens-bundle.js --target=firefox115"
  },
  "dependencies": {
    "@rxliuli/chrome-lens-ocr": "4.1.1"
  },
  "devDependencies": {
    "esbuild": "0.24.0"
  }
}
